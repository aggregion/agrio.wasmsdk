include(ExternalProject)
find_package(Git REQUIRED)
include(GNUInstallDirs)

ExternalProject_Add(
  AgrioClang
  CMAKE_ARGS -DCMAKE_INSTALL_PREFIX=${CMAKE_INSTALL_PREFIX}/llvm -DCMAKE_BUILD_TYPE=Release -DAGRIO_VER_MAJOR=${VERSION_MAJOR} -DAGRIO_VER_MINOR=${VERSION_MINOR} -DAGRIO_VER_REVISION=${VERSION_PATCH} -DAGRIO_STACK_SIZE=8192

  SOURCE_DIR "${CMAKE_SOURCE_DIR}/agrio_llvm"
  UPDATE_COMMAND ""
  PATCH_COMMAND  ""
  TEST_COMMAND   ""
  INSTALL_COMMAND ""
  BUILD_ALWAYS 1
)
