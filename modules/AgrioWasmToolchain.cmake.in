message(STATUS "Setting up Agrio Wasm Toolchain")
set(CMAKE_SYSTEM_NAME WebAssembly)

set(AGRIO_CDT_VERSION "@VERSION_MAJOR@.@VERSION_MINOR@.@VERSION_PATCH@")
set(AGRIO_WASMSDK_VERSION "@VERSION_MAJOR@.@VERSION_MINOR@.@VERSION_PATCH@")

set(CLANG_DIR @CMAKE_INSTALL_PREFIX@/)

set(CMAKE_C_COMPILER "${CLANG_DIR}/bin/agrio-cc")
set(CMAKE_CXX_COMPILER "${CLANG_DIR}/bin/agrio-cpp")

set(CMAKE_C_FLAGS " -O3 -I@BOOST_ROOT@/include ")
set(CMAKE_CXX_FLAGS " -O3 -I@BOOST_ROOT@/include ")

set(WASM_LINKER "${CLANG_DIR}/bin/agrio-ld")

set(CMAKE_C_LINK_EXECUTABLE "${WASM_LINKER} <LINK_FLAGS> <OBJECTS> -o <TARGET> <LINK_LIBRARIES>")
set(CMAKE_CXX_LINK_EXECUTABLE "${WASM_LINKER} <LINK_FLAGS> <OBJECTS> -o <TARGET> <LINK_LIBRARIES>")

set(CMAKE_AR "${CLANG_DIR}/bin/agrio-ar" CACHE PATH "ar" FORCE)
set(CMAKE_RANLIB "${CLANG_DIR}/bin/agrio-ranlib" CACHE PATH "ranlib" FORCE)

# hack for CMake on Linux
set(CMAKE_SHARED_LIBRARY_LINK_C_FLAGS)
set(CMAKE_SHARED_LIBRARY_LINK_CXX_FLAGS)

# hack for OSX
set(CMAKE_OSX_SYSROOT="${CLANG_DIR}")
set(CMAKE_OSX_DEPLOYMENT_TARGET="")
